# data-stage analysis (notebooks 00, 01, 02, 03, 04, 05)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
statsmodels>=0.14

# running the notebooks
jupyter>=1.0
nbconvert>=7.0

# numerical dependencies used by the archived CMM work and current sparse preference code
scipy>=1.11
scikit-learn>=1.3
threadpoolctl>=3.0  # deterministic single-threaded BLAS scope in Stage 1 fold-in
cvxpy>=1.4          # ships the SCS SDP solver, used for the CMM demand SDP cross-check
pytest>=8.0         # src/ unit tests (tests/)
implicit==0.7.2     # frozen Stage 1 weighted implicit-ALS backend
psutil>=5.9         # Stage 1 process-level runtime and memory trace
